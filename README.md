# Orange3 Web Backend

FastAPI backend server for Orange3 Web - providing REST API and WebSocket endpoints for Orange3 data mining widgets.

## Quick Start

```bash
uv venv --python 3.12
source .venv/bin/activate
uv pip install -e .
uvicorn app.main:app --reload --port 8000
```
