# Orange3-Web Backend Requirements
# Python 3.12 + Conda 호환 (2024-12-31)
#
# NOTE: Orange3, orange3-text, PyQt는 Conda로 설치됨
# 이 파일은 pip으로 설치할 추가 패키지만 포함

# FastAPI Backend
fastapi>=0.128.0
uvicorn[standard]>=0.40.0
pydantic>=2.12.0
python-multipart>=0.0.21
websockets>=15.0

# HTTP client
httpx>=0.28.0

# Database (SQLite + async)
sqlalchemy>=2.0.45
aiosqlite>=0.22.0
greenlet>=3.3.0
